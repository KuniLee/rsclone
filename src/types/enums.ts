export enum Paths {
    Root = '/',
    UserAccount = '/user',
    UsersList = '/users',
    News = '/news',
    Hub = '/hub',
    Sandbox = '/sandbox',
    All = '/all',
    Flows = '/flows',
    Auth = '/login',
    Settings = '/settings',
    Registration = '/register',
    Search = '/search',
}

export enum Sandbox {
    New = '/new',
}

export enum Flows {
    All = '/all',
    Develop = '/develop',
    Admin = '/admin',
    Design = '/design',
}

export enum SettingsPaths {
    Profile = '/profile',
}
