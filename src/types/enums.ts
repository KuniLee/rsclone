export enum Paths {
    Root = '/',
    Feed = '/feed',
    UserAccount = '/user',
    UsersList = '/users',
    News = '/news',
    Hub = '/hub',
    Sandbox = '/sandbox',
    All = '/all',
    Flows = '/flows',
    Auth = '/auth.html',
    Registration = '/register',
}

export enum Flows {
    All = '/all',
    Develop = '/develop',
    Admin = '/admin',
}
