export enum Paths {
    Root = '/',
    Feed = '/feed',
    UserAccount = '/user',
    UsersList = '/users',
    News = '/news',
    Hub = '/hub',
    Sandbox = '/sandbox',
    All = '/all',
    Flows = '/flows',
    Auth = '/auth',
}

export enum Flows {
    All = '/all',
    Develop = '/develop',
    Admin = '/admin',
}
